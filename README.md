# Задание 1
## Задание: функция для поиска значения в сложном объекте.

### Рекурсия
1. Начинает перебирать элементы текущего списка по порядку.
2. Сравнивает каждый элемент с искомым.
3. Если находит совпадение, вычисляет и возвращает его полный индекс.
4. Если текущий элемент — список, вызывает саму себя для этого вложенного списка, передавая обновленный накопленный индекс.
5. Ждет результат от вложенного поиска; если элемент найден в глубине, возвращает этот результат.
6. Если после проверки всех элементов (и всех вложенных списков) ничего не найдено, возвращает None.

###### Результат:
```python
print(find([1, 2, [3, 4, [5, [6, []]]]], 4)) 
print(find([1, 2, [3, 4, [5, [6, []]]]], 'spam')) 
```
<img width="201" height="65" alt="image" src="https://github.com/user-attachments/assets/496c3d91-0384-4d1a-abb0-ab1b10d0d30a" />

### Без рекурсии
1. Создает стек и помещает в него исходный список с начальным индексом 0.
2. Запускает цикл: пока в стеке есть элементы, достает из него последний список и его базовый индекс.
3. Перебирает все элементы этого списка.
4. Если элемент равен искомому, возвращает сумму базового индекса и текущего индекса элемента.
5. Если элемент сам является списком, добавляет его в стек вместе с обновленным базовым индексом (базовый + текущий индекс).
6. Возвращается к шагу 2, пока стек не опустеет.
7. Если ничего не найдено, возвращает None.

###### Результат:
```python
print(find([1, 2, [3, 4, [5, [6, []]]]], 4)) 
print(find([1, 2, [3, 4, [5, [6, []]]]], 'spam')) 
```

<img width="201" height="65" alt="image" src="https://github.com/user-attachments/assets/8e24feaa-ed51-49a3-a01b-a8e06fa9ad72" />


# Задание 2
## Задание:
Пусть заданы начальные значения: <br>
a₁ = u, b₁ = v
<br>
Тогда последующие члены последовательности вычисляются по формулам:<br>
aₖ = 2bₖ₋₁ + aₖ₋₁   <br>
bₖ = 2bₖ₋₁² + bₖ₋₁   <br>
### Рекурсия
1. Принимает начальные значения u, v и номер шага k.
2. Если k равен 1, возвращает (u, v).
3. Если k больше 1, рекурсивно вызывает саму себя с k-1, чтобы получить предыдущие значения aₖ₋₁ и bₖ₋₁ .
4. Вычисляет aₖ по формуле 2bₖ₋₁ + aₖ₋₁.
5. Вычисляет bₖ по формуле 2bₖ₋₁² + bₖ₋₁.
6. Возвращает пару (aₖ, bₖ).
###### Результат:
```python
print(calc(3, 2, 4))
```
<img width="304" height="45" alt="image" src="https://github.com/user-attachments/assets/b7d0339e-1431-4d7f-b850-57ce16bfc384" />

### Без рекурсии
1. Присваивает начальные значения: a = u, b = v
2. Запускает цикл от 2 до k включительно
3. На каждой итерации вычисляет новые значения
4. Обновляет a и b на только что вычисленные значения
5. После завершения цикла возвращает финальные (a, b)
###### Результат:
```python
print(calc(3, 2, 4))
```
<img width="304" height="45" alt="image" src="https://github.com/user-attachments/assets/947ea03c-26dd-4581-bffb-2ef7a0a54287" />

# Список использованных источников:
1. [Recursion in Programming - Full Course - freeCodeCamp.org](https://www.youtube.com/watch?v=IJDJ0kBx2LM)
2. [🐍 Самоучитель по Python для начинающих. Часть 13: Рекурсивные функции - proglib.io](https://proglib.io/p/samouchitel-po-python-dlya-nachinayushchih-chast-13-rekursivnye-funkcii-2023-01-23)
3. [Как работает рекурсия – объяснение в блок-схемах и видео - Хабр](https://habr.com/ru/articles/337030/)
